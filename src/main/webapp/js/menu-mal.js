/*-------动态菜单------*/	var menu_mal=function(){		jQuery("#menu ul li a").wrapInner('<span class="out"></span>').append(				'<span class="bg"></span>');		// loop each anchor and add copy of text content		jQuery("#menu ul li a").each(				function() {					jQuery(							'<span class="over">' + jQuery(this).text()									+ '</span>').appendTo(this);				});		jQuery("#menu ul li a").hover(function() {			// this function is fired when the mouse is moved over			jQuery(".out", this).stop().animate({				'top' : '45px'			}, 250); // move down - hide			jQuery(".over", this).stop().animate({				'top' : '0px'			}, 250); // move down - show			jQuery(".bg", this).stop().animate({				'top' : '0px'			}, 120); // move down - show			}, function() {			// this function is fired when the mouse is moved off			jQuery(".out", this).stop().animate({				'top' : '0px'			}, 250); // move up - show			jQuery(".over", this).stop().animate({				'top' : '-45px'			}, 250); // move up - hide			jQuery(".bg", this).stop().animate({				'top' : '-45px'			}, 120); // move up - hide		});	}jQuery(document).ready(	function() {		/* 1st example */		// / wrap inner content of each anchor with first layer and append		// background layer		//jQuery(this).menu_mal();	});
